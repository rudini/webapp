import { Routes } from '@angular/router';
import { SecureRoutePageComponent } from './secure-route-page.component';

const SECURE_ROUTE_PAGE_ROUTES: Routes = [
  {
    path: '',
    component: SecureRoutePageComponent,
  },
];

export default SECURE_ROUTE_PAGE_ROUTES;
